import 'package:flutter/material.dart';

/// Menu Mate rang palitrasi — Claude Design'dan (Terracotta + krem).
class AppColors {
  AppColors._();

  // === Brand ===
  static const Color terracotta = Color(0xFFC74B3A);        // asosiy
  static const Color terracottaDark = Color(0xFF8F3327);    // hover/pressed
  static const Color saffron = Color(0xFFE8A628);
  static const Color saffronLight = Color(0xFFF5B841);

  // === Neutral fon ===
  static const Color creamBg = Color(0xFFFDF6E9);           // asosiy fon
  static const Color creamCard = Color(0xFFFFF3D6);          // ochroq karta
  static const Color creamAccent = Color(0xFFFFE4B0);        // accent karta
  static const Color surface = Color(0xFFFFFFFF);

  // === Matn ranglari ===
  static const Color textDark = Color(0xFF2B1810);           // asosiy matn
  static const Color textMedium = Color(0xFF5C4433);         // sekundar
  static const Color textLight = Color(0xFF8F7A5F);          // uchunchi
  static const Color textOnBrand = Color(0xFFFFFFFF);

  // === Semantic ===
  static const Color success = Color(0xFF7BA05B);            // yashil (sog'lom)
  static const Color warning = Color(0xFFE8A628);
  static const Color error = Color(0xFFC74B3A);              // brand xato

  // === Dark mode ===
  static const Color darkBg = Color(0xFF1A1410);
  static const Color darkSurface = Color(0xFF2B1810);
  static const Color darkCard = Color(0xFF3A2620);
}
